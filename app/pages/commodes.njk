{% extends "templates/default.njk" %}
{% block header %}
    {% include "templates/partials/header.njk" %}
{% endblock %}
{% block content %}
    <section class="images-wrap">
        <img class="header-shadow img-responsive" src="images/top_shadow.png" alt="shadow">
        {% import "templates/macros/breadcrumbs.njk" as breadcrumbs %}
        {{ breadcrumbs.page('комоды') }}
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <div class="images">
                        {% for thumb in commodes_1 %}
                            <div class="thumb">
                                <a href="{{ thumb.href }}" data-rel="lightcase:myCollection"><img src="{{ thumb.src }}" alt="{{ thumb.alt }}"></a>
                                <span class="thumb-caption">{{ thumb.number }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <div class="images">
                        {% for thumb in commodes_2 %}
                            <div class="thumb">
                                <a href="{{ thumb.href }}" data-rel="lightcase:myCollection"><img src="{{ thumb.src }}" alt="{{ thumb.alt }}"></a>
                                <span class="thumb-caption">{{ thumb.number }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <div class="images">
                        {% for thumb in commodes_3 %}
                            <div class="thumb">
                                <a href="{{ thumb.href }}" data-rel="lightcase:myCollection"><img src="{{ thumb.src }}" alt="{{ thumb.alt }}"></a>
                                <span class="thumb-caption">{{ thumb.number }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <div class="images">
                        {% for thumb in commodes_4 %}
                            <div class="thumb">
                                <a href="{{ thumb.href }}" data-rel="lightcase:myCollection"><img src="{{ thumb.src }}" alt="{{ thumb.alt }}"></a>
                                <span class="thumb-caption">{{ thumb.number }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <div class="images">
                        {% for thumb in commodes_5 %}
                            <div class="thumb">
                                <a href="{{ thumb.href }}" data-rel="lightcase:myCollection"><img src="{{ thumb.src }}" alt="{{ thumb.alt }}"></a>
                                <span class="thumb-caption">{{ thumb.number }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
        <img class="bottom-shadow img-responsive" src="images/top_shadow.png" alt="shadow">
    </section>
{% endblock %}